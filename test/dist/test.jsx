import React, { Component } from 'react';
import './index.scss';

export default class extends Component {
    constructor( props ) {
        super( props );
        const a = '.net';
        console.log( a );
    }

    render() {
        return (
            <div>
                <h1>123</h1>
            </div>
        );
    }
}